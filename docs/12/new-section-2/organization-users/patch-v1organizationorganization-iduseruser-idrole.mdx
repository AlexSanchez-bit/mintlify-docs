<Endpoint method="PATCH" path="/v1/organization/{organization_id}/user/{user_id}/role" />

## Authentication

**API Key** (`BearerAuth`)
- Header: `Authorization`

## Path Parameters

<ParamField path="organization_id" type="string" required>
  Organization ID
</ParamField>

<ParamField path="user_id" type="string" required>
  User ID
</ParamField>

## Request Body

Update role input

**Required**

<ParamField path="role" type="string" required>
</ParamField>

**Example:**

```json
{
  "role": "string"
}
```

## Responses

<Response status="200">
  OK
</Response>

**Response Fields:**

- `created_at` (string)
- `documentation_ids` (string[])
- `email` (string)
- `id` (string)
- `invited_at` (string)
- `invited_by` (string)
- `joined_at` (string)
- `name` (string)
- `organization_id` (string)
- `profile_picture_url` (string)
- `role` (string)
- `status` (string)
- `updated_at` (string)
- `user_id` (string)

**Example:**

```json
{
  "created_at": "string",
  "documentation_ids": [
    "string"
  ],
  "email": "string",
  "id": "string",
  "invited_at": "string",
  "invited_by": "string",
  "joined_at": "string",
  "name": "string",
  "organization_id": "string",
  "profile_picture_url": "string",
  "role": "admin",
  "status": "pending",
  "updated_at": "string",
  "user_id": "string"
}
```

<Response status="400">
  Bad Request
</Response>

**Example:**

```json
{
  "key": "value"
}
```

<Response status="403">
  Forbidden
</Response>

**Example:**

```json
{
  "key": "value"
}
```

<Response status="500">
  Internal Server Error
</Response>

**Example:**

```json
{
  "key": "value"
}
```
