Streams a CSV file of every customer matching the filter set. Same query params as the list endpoint.

<Endpoint method="GET" path="/v1/admin/customers/export" />

## Authentication

**API Key** (`BearerAuth`)
- Header: `Authorization`
